# Display profiles

Put MultiMonitorTool `.cfg` profiles here.

At startup on Windows, `desk-agent` extracts these files to:

```text
%APPDATA%\desk-agent\displays
```

The PowerShell actions load profiles from that directory.
